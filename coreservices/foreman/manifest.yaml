# Code generated by cmd/genservice. DO NOT EDIT.

general:
  name: Foreman
  hostname: foreman.core
  description: Foreman orchestrates agentic workflow execution.
  package: github.com/microbus-io/fabric/coreservices/foreman
  modifiedAt: "2026-08-03T01:26:47Z"

configs:
  Shards:
    signature: Shards() (value []ShardSpec)
    description: |-
      Shards declares the database shards that hold flows and steps, one entry per shard. Each carries its own
      Index (>= 1, unique, stable across restarts and identical on every replica), DSN (used verbatim, no
      templating), VirtualCPUs (the CPU count of that shard's database server, which sizes its connection budget
      and placement weight), and Cordoned (excludes the shard from new-flow placement while everything already
      resident keeps running). Shards can be added but never removed, and a change takes effect only on restart,
      after a coordinated restart of every replica. Left empty, a LOCAL deployment falls back to a single SQLite
      file shard.
    validation: json
    default: "[]"
    secret: true
  Workers:
    signature: Workers() (value int)
    description: Workers is the maximum number of concurrent workers that process flow steps. Leave at -1 to let the engine derive the ceiling from each shard's connection budget and measured round-trip time. 0 stands up a replica that creates, awaits, and serves reads but never executes a task.
    validation: int [-1,]
    default: -1
  TimeBudget:
    signature: TimeBudget() (value time.Duration)
    description: TimeBudget is the default time budget for a single task step's execution, applied as the timeout on the task dispatch call. A flow may override it per-flow via FlowOptions.TimeBudget, up to a hard 15m ceiling; a task endpoint may declare a shorter budget of its own via sub.TimeBudget.
    validation: dur [1s,15m]
    default: 2m
  DefaultPriority:
    signature: DefaultPriority() (value int)
    description: DefaultPriority is the priority assigned to a flow when the caller does not specify one. Priority is an integer >= 1, lower numbers run first.
    validation: int [1,]
    default: 5
  MaxOpenConns:
    signature: MaxOpenConns() (value int)
    description: MaxOpenConns pins every shard's connection pool to exactly this many open connections. Leave at 0 to let the engine size each pool from that shard's VirtualCPUs. Set it only when the connection budget is constrained by something the engine cannot see, such as a shared database or an external pooler.
    validation: int [0,]
    default: 0

metrics:
  AckTimeouts:
    signature: AckTimeouts(value int, task_url string, outcome string)
    description: "AckTimeouts counts task dispatches that hit a 404 ack-timeout (no microservice acked the dispatch), keyed by the task endpoint and the outcome: \"retry\" when the foreman re-probed within the step's time budget, \"giveup\" when the budget horizon was spent and the step was failed. The \"giveup\" series is the alertable \"a microservice is missing\" signal. Named to parallel the framework's microbus_client_timeout_requests; the Prometheus exporter appends the _total suffix."
    kind: counter
    otelName: microbus_foreman_timeout_requests

functions:
  Create:
    signature: Create(workflowURL string, initialState any, opts *workflow.FlowOptions) (flowKey string)
    description: Create creates a flow for a workflow and immediately runs it, returning the running flow's key. There is no separate start step. Set Opts.ThreadKey to join an existing thread; for a deferred start, have the entry task call flow.Interrupt and Resume it when ready.
    method: ANY
    route: :444/create
  Snapshot:
    signature: Snapshot(flowKey string) (outcome *workflow.FlowOutcome)
    description: Snapshot returns the current outcome of a flow.
    method: GET
    route: :444/snapshot
  Fingerprint:
    signature: Fingerprint(flowKey string) (fingerprint string, status string)
    description: Fingerprint returns a short opaque hash that changes when the flow's status, step count, or any step's updated_at changes — across the flow and any nested subgraph descendants.
    method: GET
    route: :444/fingerprint
  Resume:
    signature: Resume(flowKey string, resumeData any)
    description: Resume continues an interrupted flow, delivering resumeData to the task that armed flow.Interrupt.
    method: POST
    route: :444/resume
  Cancel:
    signature: Cancel(flowKey string, reason string)
    description: Cancel cancels a flow that is not yet in a terminal status.
    method: POST
    route: :444/cancel
  Fork:
    signature: Fork(stepKey string, stateOverrides any) (newFlowKey string)
    description: Fork clones a terminal flow's prefix up to the given step into a new, self-contained running flow and re-executes from that step with optional stateOverrides applied to it. The original flow is never modified. The fork point may be any recorded step, including one inside a subgraph. The fork inherits the origin flow's scheduling and baggage.
    method: POST
    route: :444/fork
  History:
    signature: History(flowKey string) (steps []FlowStep)
    description: History returns the step-by-step execution history of a flow.
    method: GET
    route: :444/history
  Step:
    signature: Step(stepKey string) (step *FlowStep)
    description: Step returns the full detail of one execution step, including the state, changes and interrupt payload that History omits.
    method: GET
    route: :444/step
  List:
    signature: List(query Query) (flows []FlowSummary, nextCursor string)
    description: List queries flows by status or workflow URL. Set Query.Cursor to the previous call's NextCursor to paginate.
    method: GET
    route: :444/list
  Delete:
    signature: Delete(flowKey string)
    description: Delete removes a flow and its steps from the database. The flow must not be running. Subgraph and thread lineage references become dangling.
    method: POST
    route: :444/delete
  Purge:
    signature: Purge(query Query) (deleted int)
    description: Purge deletes flows matching the query, except those currently running. Capped at 10000 flows per call.
    method: POST
    route: :444/purge
  ShardInfo:
    signature: ShardInfo() (shards []ShardSummary)
    description: ShardInfo returns per-shard health (latency, row counts, error) for every database shard.
    method: GET
    route: :444/shard-info
  Await:
    signature: Await(flowKey string) (outcome *workflow.FlowOutcome)
    description: Await blocks until the flow stops (i.e. is no longer created, pending, or running), then returns the outcome.
    method: POST
    route: :444/wait-for-stop
  Poll:
    signature: Poll(flowKey string) (outcome *workflow.FlowOutcome)
    description: |-
      Poll returns a flow's current outcome, waiting up to the request's time budget for it to stop. Unlike Await, a
      timeout is not an error: a still-running flow returns a running outcome (Outcome.Stopped() is false) so a caller
      bridging an open-ended flow to a bounded request can answer within its budget and re-poll immediately.
    method: POST
    route: :444/poll
  Run:
    signature: Run(workflowURL string, initialState any, opts *workflow.FlowOptions) (outcome *workflow.FlowOutcome)
    description: Run creates a new flow, starts it, and blocks until it stops. Returns the terminal outcome.
    method: POST
    route: :444/run
  Continue:
    signature: Continue(threadKey string, additionalState any) (newFlowKey string)
    description: Continue creates a new running flow from the latest completed flow in a thread, merged with additional state using the graph's reducers. The threadKey can be any flowKey belonging to the thread. The new flow belongs to the same thread and inherits its policy (priority/fairness/budget/baggage); use Create with Opts.ThreadKey to set policy explicitly instead.
    method: POST
    route: :444/continue

webs:
  HistoryMermaid:
    description: HistoryMermaid renders an HTML page with a Mermaid diagram of the flow's execution history.
    method: GET
    route: :444/history-mermaid
