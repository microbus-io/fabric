// Code generated by cmd/genservice. DO NOT EDIT.

package foreman

import (
	"context"
	"net/http"
	"testing"

	"github.com/microbus-io/dwarf/workflow"
	"github.com/microbus-io/fabric/connector"
	"github.com/microbus-io/fabric/coreservices/foreman/foremanapi"
	"github.com/microbus-io/fabric/httpx"
	"github.com/microbus-io/testarossa"
)

func TestForeman_Mock(t *testing.T) {
	t.Parallel()
	ctx := t.Context()

	mock := NewMock()
	mock.SetDeployment(connector.TESTING)

	t.Run("on_startup", func(t *testing.T) {
		assert := testarossa.For(t)
		err := mock.OnStartup(ctx)
		assert.NoError(err)
	})

	t.Run("on_shutdown", func(t *testing.T) {
		assert := testarossa.For(t)
		err := mock.OnShutdown(ctx)
		assert.NoError(err)
	})

	t.Run("create", func(t *testing.T) { // MARKER: Create
		assert := testarossa.For(t)

		mock.MockCreate(func(ctx context.Context, workflowURL string, initialState any, opts *workflow.FlowOptions) (flowKey string, err error) {
			return
		})
		var workflowURL string
		var initialState any
		var opts *workflow.FlowOptions
		_, err := mock.Create(ctx, workflowURL, initialState, opts)
		assert.NoError(err)
	})

	t.Run("snapshot", func(t *testing.T) { // MARKER: Snapshot
		assert := testarossa.For(t)

		mock.MockSnapshot(func(ctx context.Context, flowKey string) (outcome *workflow.FlowOutcome, err error) {
			return
		})
		var flowKey string
		_, err := mock.Snapshot(ctx, flowKey)
		assert.NoError(err)
	})

	t.Run("fingerprint", func(t *testing.T) { // MARKER: Fingerprint
		assert := testarossa.For(t)

		mock.MockFingerprint(func(ctx context.Context, flowKey string) (fingerprint string, status string, err error) {
			return
		})
		var flowKey string
		_, _, err := mock.Fingerprint(ctx, flowKey)
		assert.NoError(err)
	})

	t.Run("resume", func(t *testing.T) { // MARKER: Resume
		assert := testarossa.For(t)

		mock.MockResume(func(ctx context.Context, flowKey string, resumeData any) (err error) {
			return
		})
		var flowKey string
		var resumeData any
		err := mock.Resume(ctx, flowKey, resumeData)
		assert.NoError(err)
	})

	t.Run("cancel", func(t *testing.T) { // MARKER: Cancel
		assert := testarossa.For(t)

		mock.MockCancel(func(ctx context.Context, flowKey string, reason string) (err error) {
			return
		})
		var flowKey string
		var reason string
		err := mock.Cancel(ctx, flowKey, reason)
		assert.NoError(err)
	})

	t.Run("fork", func(t *testing.T) { // MARKER: Fork
		assert := testarossa.For(t)

		mock.MockFork(func(ctx context.Context, stepKey string, stateOverrides any) (newFlowKey string, err error) {
			return
		})
		var stepKey string
		var stateOverrides any
		_, err := mock.Fork(ctx, stepKey, stateOverrides)
		assert.NoError(err)
	})

	t.Run("history", func(t *testing.T) { // MARKER: History
		assert := testarossa.For(t)

		mock.MockHistory(func(ctx context.Context, flowKey string) (steps []foremanapi.FlowStep, err error) {
			return
		})
		var flowKey string
		_, err := mock.History(ctx, flowKey)
		assert.NoError(err)
	})

	t.Run("step", func(t *testing.T) { // MARKER: Step
		assert := testarossa.For(t)

		mock.MockStep(func(ctx context.Context, stepKey string) (step *foremanapi.FlowStep, err error) {
			return
		})
		var stepKey string
		_, err := mock.Step(ctx, stepKey)
		assert.NoError(err)
	})

	t.Run("list", func(t *testing.T) { // MARKER: List
		assert := testarossa.For(t)

		mock.MockList(func(ctx context.Context, query foremanapi.Query) (flows []foremanapi.FlowSummary, nextCursor string, err error) {
			return
		})
		var query foremanapi.Query
		_, _, err := mock.List(ctx, query)
		assert.NoError(err)
	})

	t.Run("delete", func(t *testing.T) { // MARKER: Delete
		assert := testarossa.For(t)

		mock.MockDelete(func(ctx context.Context, flowKey string) (err error) {
			return
		})
		var flowKey string
		err := mock.Delete(ctx, flowKey)
		assert.NoError(err)
	})

	t.Run("purge", func(t *testing.T) { // MARKER: Purge
		assert := testarossa.For(t)

		mock.MockPurge(func(ctx context.Context, query foremanapi.Query) (deleted int, err error) {
			return
		})
		var query foremanapi.Query
		_, err := mock.Purge(ctx, query)
		assert.NoError(err)
	})

	t.Run("shard_info", func(t *testing.T) { // MARKER: ShardInfo
		assert := testarossa.For(t)

		mock.MockShardInfo(func(ctx context.Context) (shards []foremanapi.ShardSummary, err error) {
			return
		})
		_, err := mock.ShardInfo(ctx)
		assert.NoError(err)
	})

	t.Run("await", func(t *testing.T) { // MARKER: Await
		assert := testarossa.For(t)

		mock.MockAwait(func(ctx context.Context, flowKey string) (outcome *workflow.FlowOutcome, err error) {
			return
		})
		var flowKey string
		_, err := mock.Await(ctx, flowKey)
		assert.NoError(err)
	})

	t.Run("poll", func(t *testing.T) { // MARKER: Poll
		assert := testarossa.For(t)

		mock.MockPoll(func(ctx context.Context, flowKey string) (outcome *workflow.FlowOutcome, err error) {
			return
		})
		var flowKey string
		_, err := mock.Poll(ctx, flowKey)
		assert.NoError(err)
	})

	t.Run("run", func(t *testing.T) { // MARKER: Run
		assert := testarossa.For(t)

		mock.MockRun(func(ctx context.Context, workflowURL string, initialState any, opts *workflow.FlowOptions) (outcome *workflow.FlowOutcome, err error) {
			return
		})
		var workflowURL string
		var initialState any
		var opts *workflow.FlowOptions
		_, err := mock.Run(ctx, workflowURL, initialState, opts)
		assert.NoError(err)
	})

	t.Run("continue", func(t *testing.T) { // MARKER: Continue
		assert := testarossa.For(t)

		mock.MockContinue(func(ctx context.Context, threadKey string, additionalState any) (newFlowKey string, err error) {
			return
		})
		var threadKey string
		var additionalState any
		_, err := mock.Continue(ctx, threadKey, additionalState)
		assert.NoError(err)
	})

	t.Run("history_mermaid", func(t *testing.T) { // MARKER: HistoryMermaid
		assert := testarossa.For(t)

		mock.MockHistoryMermaid(func(w http.ResponseWriter, r *http.Request) (err error) {
			return nil
		})
		w := httpx.NewResponseRecorder()
		r := httpx.MustNewRequest("GET", "/", nil)
		err := mock.HistoryMermaid(w, r)
		assert.NoError(err)
	})

}
