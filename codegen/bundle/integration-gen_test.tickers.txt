{{ range .Tickers }}
// {{ .Name }}TestCase assists in asserting against the results of executing {{ .Name }}.
type {{ .Name }}TestCase struct {
	err error
}

// Error asserts an error.
func (tc *{{ .Name }}TestCase) Error(t *testing.T, errContains string) *{{ .Name }}TestCase {
	if assert.Error(t, tc.err) {
		assert.Contains(t, tc.err.Error(), errContains)
	}
	return tc
}

// NoError asserts no error.
func (tc *{{ .Name }}TestCase) NoError(t *testing.T) *{{ .Name }}TestCase {
	assert.NoError(t, tc.err)
	return tc
}

// Assert asserts using a provided function.
func (tc *{{ .Name }}TestCase) Assert(t *testing.T, asserter func(t *testing.T, err error)) *{{ .Name }}TestCase {
	asserter(t, tc.err)
	return tc
}

// Get returns the result of executing {{ .Name }}.
func (tc *{{ .Name }}TestCase) Get() (err error) {
	return tc.err
}

// {{ .Name }} executes the ticker and returns a corresponding test case.
func {{ .Name }}(ctx context.Context) *{{ .Name }}TestCase {
	tc := &{{ .Name }}TestCase{}
	tc.err = utils.CatchPanic(func () error {
		return Svc.{{ .Name }}(ctx)
	})
	return tc
}
{{ end }}