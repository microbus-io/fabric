// Code generated by cmd/genservice. DO NOT EDIT.

package banksupportapi

import (
	"context"
	"encoding/json"
	"iter"
	"net/http"
	"reflect"

	"github.com/microbus-io/dwarf/workflow"
	"github.com/microbus-io/errors"
	"github.com/microbus-io/fabric/httpx"
	"github.com/microbus-io/fabric/pub"
	"github.com/microbus-io/fabric/service"
)

// multicastResponse packs the response of a functional multicast.
type multicastResponse struct {
	data         any
	HTTPResponse *http.Response
	err          error
}

// Client is a lightweight proxy for making unicast calls to the microservice.
type Client struct {
	svc  service.Publisher
	host string
	opts []pub.Option
}

// NewClient creates a new unicast client proxy to the microservice.
func NewClient(caller service.Publisher) Client {
	return Client{svc: caller, host: Hostname}
}

// ForHost returns a copy of the client with a different hostname to be applied to requests.
func (_c Client) ForHost(host string) Client {
	return Client{svc: _c.svc, host: host, opts: _c.opts}
}

// WithOptions returns a copy of the client with options to be applied to requests.
func (_c Client) WithOptions(opts ...pub.Option) Client {
	return Client{svc: _c.svc, host: _c.host, opts: append(_c.opts, opts...)}
}

// MulticastClient is a lightweight proxy for making multicast calls to the microservice.
type MulticastClient struct {
	svc  service.Publisher
	host string
	opts []pub.Option
}

// NewMulticastClient creates a new multicast client proxy to the microservice.
func NewMulticastClient(caller service.Publisher) MulticastClient {
	return MulticastClient{svc: caller, host: Hostname}
}

// ForHost returns a copy of the client with a different hostname to be applied to requests.
func (_c MulticastClient) ForHost(host string) MulticastClient {
	return MulticastClient{svc: _c.svc, host: host, opts: _c.opts}
}

// WithOptions returns a copy of the client with options to be applied to requests.
func (_c MulticastClient) WithOptions(opts ...pub.Option) MulticastClient {
	return MulticastClient{svc: _c.svc, host: _c.host, opts: append(_c.opts, opts...)}
}

// WorkflowRunner executes a workflow by name with initial state, blocking until termination.
// foremanapi.Client satisfies this interface.
type WorkflowRunner interface {
	Run(ctx context.Context, workflowName string, initialState any, opts *workflow.FlowOptions) (outcome *workflow.FlowOutcome, err error)
}

// Executor runs tasks and workflows synchronously, blocking until termination.
// It is primarily intended for integration tests.
type Executor struct {
	svc         service.Publisher
	host        string
	opts        []pub.Option
	inFlow      *workflow.RawFlow
	outFlow     *workflow.Flow
	runner      WorkflowRunner
	flowOptions *workflow.FlowOptions
}

// NewExecutor creates a new executor proxy to the microservice.
func NewExecutor(caller service.Publisher) Executor {
	return Executor{svc: caller, host: Hostname}
}

// ForHost returns a copy of the executor with a different hostname to be applied to requests.
func (_c Executor) ForHost(host string) Executor {
	return Executor{svc: _c.svc, host: host, opts: _c.opts, inFlow: _c.inFlow, outFlow: _c.outFlow, runner: _c.runner, flowOptions: _c.flowOptions}
}

// WithOptions returns a copy of the executor with options to be applied to requests.
func (_c Executor) WithOptions(opts ...pub.Option) Executor {
	return Executor{svc: _c.svc, host: _c.host, opts: append(_c.opts, opts...), inFlow: _c.inFlow, outFlow: _c.outFlow, runner: _c.runner, flowOptions: _c.flowOptions}
}

// WithInputFlow returns a copy of the executor with an input flow to use for task execution.
// The input flow's state is available to the task in addition to the typed input arguments.
func (_c Executor) WithInputFlow(flow *workflow.RawFlow) Executor {
	return Executor{svc: _c.svc, host: _c.host, opts: _c.opts, inFlow: flow, outFlow: _c.outFlow, runner: _c.runner, flowOptions: _c.flowOptions}
}

// WithOutputFlow returns a copy of the executor with an output flow to populate after task execution.
// The output flow captures the full flow state including control signals (Goto, Retry, Interrupt, Sleep).
func (_c Executor) WithOutputFlow(flow *workflow.Flow) Executor {
	return Executor{svc: _c.svc, host: _c.host, opts: _c.opts, inFlow: _c.inFlow, outFlow: flow, runner: _c.runner, flowOptions: _c.flowOptions}
}

// WithWorkflowRunner returns a copy of the executor with a workflow runner for executing workflows.
// foremanapi.NewClient(svc) satisfies the WorkflowRunner interface.
func (_c Executor) WithWorkflowRunner(runner WorkflowRunner) Executor {
	return Executor{svc: _c.svc, host: _c.host, opts: _c.opts, inFlow: _c.inFlow, outFlow: _c.outFlow, runner: runner, flowOptions: _c.flowOptions}
}

// WithFlowOptions returns a copy of the executor that creates workflows with the given flow options
// (priority, fairness key and weight). It has no effect on task execution.
func (_c Executor) WithFlowOptions(flowOptions *workflow.FlowOptions) Executor {
	return Executor{svc: _c.svc, host: _c.host, opts: _c.opts, inFlow: _c.inFlow, outFlow: _c.outFlow, runner: _c.runner, flowOptions: flowOptions}
}

// Subgraph runs this microservice's workflows from inside a task body. Create it with
// NewSubgraph(flow), then call the method named after the workflow you want to invoke.
type Subgraph struct {
	flow *workflow.Flow
}

// NewSubgraph creates a subgraph client bound to the calling task's flow carrier.
func NewSubgraph(flow *workflow.Flow) Subgraph {
	return Subgraph{flow: flow}
}

// marshalRequest supports functional endpoints.
func marshalRequest(ctx context.Context, svc service.Publisher, opts []pub.Option, host string, method string, route string, in any, out any) (err error) {
	if method == "ANY" {
		method = "POST"
	}
	u := httpx.JoinHostAndPath(host, route)
	query, body, err := httpx.WriteInputPayload(method, in)
	if err != nil {
		return err // No trace
	}
	httpRes, err := svc.Request(
		ctx,
		pub.Method(method),
		pub.URL(u),
		pub.Query(query),
		pub.Body(body),
		pub.Options(opts...),
	)
	if err != nil {
		return err // No trace
	}
	err = httpx.ReadOutputPayload(httpRes, out)
	return errors.Trace(err)
}

// marshalPublish supports multicast functional endpoints.
func marshalPublish(ctx context.Context, svc service.Publisher, opts []pub.Option, host string, method string, route string, in any, out any) iter.Seq[*multicastResponse] {
	if method == "ANY" {
		method = "POST"
	}
	u := httpx.JoinHostAndPath(host, route)
	query, body, err := httpx.WriteInputPayload(method, in)
	if err != nil {
		return func(yield func(*multicastResponse) bool) {
			yield(&multicastResponse{err: err})
		}
	}
	_queue := svc.Publish(
		ctx,
		pub.Method(method),
		pub.URL(u),
		pub.Query(query),
		pub.Body(body),
		pub.Options(opts...),
	)
	return func(yield func(*multicastResponse) bool) {
		for qi := range _queue {
			httpResp, err := qi.Get()
			if err == nil {
				reflect.ValueOf(out).Elem().SetZero()
				err = httpx.ReadOutputPayload(httpResp, out)
			}
			if err != nil {
				if !yield(&multicastResponse{err: err, HTTPResponse: httpResp}) {
					return
				}
			} else {
				if !yield(&multicastResponse{data: out, HTTPResponse: httpResp}) {
					return
				}
			}
		}
	}
}

// marshalTask supports task execution via the Executor.
func marshalTask(ctx context.Context, svc service.Publisher, opts []pub.Option, host string, method string, route string, in any, out any, inFlow *workflow.RawFlow, outFlow *workflow.Flow) (err error) {
	flow := inFlow
	if flow == nil {
		flow = workflow.NewRawFlow()
	}
	inState, err := workflow.NewState(in)
	if err != nil {
		return errors.Trace(err)
	}
	state := flow.RawState()
	err = state.Merge(inState)
	if err != nil {
		return errors.Trace(err)
	}
	flow.SetRawState(state)
	body, err := json.Marshal(flow)
	if err != nil {
		return errors.Trace(err)
	}
	u := httpx.JoinHostAndPath(host, route)
	httpRes, err := svc.Request(
		ctx,
		pub.Method(method),
		pub.URL(u),
		pub.Body(body),
		pub.ContentType("application/json"),
		pub.Options(opts...),
	)
	if err != nil {
		return err // No trace
	}
	err = json.NewDecoder(httpRes.Body).Decode(flow)
	if err != nil {
		return errors.Trace(err)
	}
	if outFlow != nil {
		*outFlow = flow.Flow
	}
	if out != nil {
		err = flow.ParseState(out)
		return errors.Trace(err)
	}
	return nil
}

// marshalWorkflow supports workflow execution via the Executor.
func marshalWorkflow(ctx context.Context, runner WorkflowRunner, flowOptions *workflow.FlowOptions, workflowURL string, in any, out any) (status string, err error) {
	outcome, err := runner.Run(ctx, workflowURL, in, flowOptions)
	if err != nil {
		if outcome != nil {
			return outcome.Status, err // No trace
		}
		return "", err // No trace
	}
	if outcome == nil {
		return "", nil
	}
	status = outcome.Status
	if out != nil {
		err = outcome.State.Parse(out)
		if err != nil {
			return status, errors.Trace(err)
		}
	}
	return status, nil
}

// marshalSubgraph runs a child workflow via the flow carrier and returns the parker's yield.
func marshalSubgraph(flow *workflow.Flow, url string, in any, out any) (yield bool, err error) {
	if flow == nil {
		return false, errors.New("Subgraph requires a flow carrier (call from a task body)")
	}
	return flow.Subgraph(url, in, out)
}

// Balance returns the signed-in customer's current balance. It derives the customer from the actor claim, never
// from an argument, and is exposed to the LLM as a tool.
func (_c Client) Balance(ctx context.Context) (balanceCents int, holder string, err error) { // MARKER: Balance
	_in := BalanceIn{}
	_out := BalanceOut{}
	err = marshalRequest(ctx, _c.svc, _c.opts, _c.host, Balance.Method, Balance.Route, &_in, &_out)
	return _out.BalanceCents, _out.Holder, err // No trace
}

// BalanceResponse packs the response of Balance.
type BalanceResponse multicastResponse // MARKER: Balance

// Get unpacks the return arguments of Balance.
func (_res *BalanceResponse) Get() (balanceCents int, holder string, err error) { // MARKER: Balance
	_d := _res.data.(*BalanceOut)
	return _d.BalanceCents, _d.Holder, _res.err
}

// Balance returns the signed-in customer's current balance. It derives the customer from the actor claim, never
// from an argument, and is exposed to the LLM as a tool.
func (_c MulticastClient) Balance(ctx context.Context) iter.Seq[*BalanceResponse] { // MARKER: Balance
	_in := BalanceIn{}
	_out := BalanceOut{}
	_queue := marshalPublish(ctx, _c.svc, _c.opts, _c.host, Balance.Method, Balance.Route, &_in, &_out)
	return func(yield func(*BalanceResponse) bool) {
		for _r := range _queue {
			_clone := _out
			_r.data = &_clone
			if !yield((*BalanceResponse)(_r)) {
				return
			}
		}
	}
}

// Transactions returns the signed-in customer's transactions in a date range. It derives the customer from the
// actor claim, never from an argument, and is exposed to the LLM as a tool for spend analysis over time.
func (_c Client) Transactions(ctx context.Context, fromDate string, toDate string) (transactions []TxnView, err error) { // MARKER: Transactions
	_in := TransactionsIn{FromDate: fromDate, ToDate: toDate}
	_out := TransactionsOut{}
	err = marshalRequest(ctx, _c.svc, _c.opts, _c.host, Transactions.Method, Transactions.Route, &_in, &_out)
	return _out.Transactions, err // No trace
}

// TransactionsResponse packs the response of Transactions.
type TransactionsResponse multicastResponse // MARKER: Transactions

// Get unpacks the return arguments of Transactions.
func (_res *TransactionsResponse) Get() (transactions []TxnView, err error) { // MARKER: Transactions
	_d := _res.data.(*TransactionsOut)
	return _d.Transactions, _res.err
}

// Transactions returns the signed-in customer's transactions in a date range. It derives the customer from the
// actor claim, never from an argument, and is exposed to the LLM as a tool for spend analysis over time.
func (_c MulticastClient) Transactions(ctx context.Context, fromDate string, toDate string) iter.Seq[*TransactionsResponse] { // MARKER: Transactions
	_in := TransactionsIn{FromDate: fromDate, ToDate: toDate}
	_out := TransactionsOut{}
	_queue := marshalPublish(ctx, _c.svc, _c.opts, _c.host, Transactions.Method, Transactions.Route, &_in, &_out)
	return func(yield func(*TransactionsResponse) bool) {
		for _r := range _queue {
			_clone := _out
			_r.data = &_clone
			if !yield((*TransactionsResponse)(_r)) {
				return
			}
		}
	}
}

// DemoStatus long-polls a launched Support workflow via the foreman's Poll and returns the structured verdict
// when the flow has stopped, or a running status so the demo page can re-poll immediately with no client delay.
func (_c Client) DemoStatus(ctx context.Context, flowKey string) (status string, advice string, blockCard bool, risk int, errorMsg string, err error) { // MARKER: DemoStatus
	_in := DemoStatusIn{FlowKey: flowKey}
	_out := DemoStatusOut{}
	err = marshalRequest(ctx, _c.svc, _c.opts, _c.host, DemoStatus.Method, DemoStatus.Route, &_in, &_out)
	return _out.Status, _out.Advice, _out.BlockCard, _out.Risk, _out.ErrorMsg, err // No trace
}

// DemoStatusResponse packs the response of DemoStatus.
type DemoStatusResponse multicastResponse // MARKER: DemoStatus

// Get unpacks the return arguments of DemoStatus.
func (_res *DemoStatusResponse) Get() (status string, advice string, blockCard bool, risk int, errorMsg string, err error) { // MARKER: DemoStatus
	_d := _res.data.(*DemoStatusOut)
	return _d.Status, _d.Advice, _d.BlockCard, _d.Risk, _d.ErrorMsg, _res.err
}

// DemoStatus long-polls a launched Support workflow via the foreman's Poll and returns the structured verdict
// when the flow has stopped, or a running status so the demo page can re-poll immediately with no client delay.
func (_c MulticastClient) DemoStatus(ctx context.Context, flowKey string) iter.Seq[*DemoStatusResponse] { // MARKER: DemoStatus
	_in := DemoStatusIn{FlowKey: flowKey}
	_out := DemoStatusOut{}
	_queue := marshalPublish(ctx, _c.svc, _c.opts, _c.host, DemoStatus.Method, DemoStatus.Route, &_in, &_out)
	return func(yield func(*DemoStatusResponse) bool) {
		for _r := range _queue {
			_clone := _out
			_r.data = &_clone
			if !yield((*DemoStatusResponse)(_r)) {
				return
			}
		}
	}
}

// Login renders the login screen and, on valid credentials, mints a customer bearer token and redirects to the demo.
func (_c Client) Login(ctx context.Context, method string, relativeURL string, body any) (res *http.Response, err error) { // MARKER: Login
	if method == "" {
		method = Login.Method
	}
	if method == "ANY" {
		method = "POST"
	}
	return _c.svc.Request(
		ctx,
		pub.Method(method),
		pub.URL(httpx.JoinHostAndPath(_c.host, Login.Route)),
		pub.RelativeURL(relativeURL),
		pub.Body(body),
		pub.Options(_c.opts...),
	)
}

// Login renders the login screen and, on valid credentials, mints a customer bearer token and redirects to the demo.
func (_c MulticastClient) Login(ctx context.Context, method string, relativeURL string, body any) iter.Seq[*pub.Response] { // MARKER: Login
	if method == "" {
		method = Login.Method
	}
	if method == "ANY" {
		method = "POST"
	}
	return _c.svc.Publish(
		ctx,
		pub.Method(method),
		pub.URL(httpx.JoinHostAndPath(_c.host, Login.Route)),
		pub.RelativeURL(relativeURL),
		pub.Body(body),
		pub.Options(_c.opts...),
	)
}

// Logout clears the customer session cookie and returns to the login screen.
func (_c Client) Logout(ctx context.Context, method string, relativeURL string, body any) (res *http.Response, err error) { // MARKER: Logout
	if method == "" {
		method = Logout.Method
	}
	if method == "ANY" {
		method = "POST"
	}
	return _c.svc.Request(
		ctx,
		pub.Method(method),
		pub.URL(httpx.JoinHostAndPath(_c.host, Logout.Route)),
		pub.RelativeURL(relativeURL),
		pub.Body(body),
		pub.Options(_c.opts...),
	)
}

// Logout clears the customer session cookie and returns to the login screen.
func (_c MulticastClient) Logout(ctx context.Context, method string, relativeURL string, body any) iter.Seq[*pub.Response] { // MARKER: Logout
	if method == "" {
		method = Logout.Method
	}
	if method == "ANY" {
		method = "POST"
	}
	return _c.svc.Publish(
		ctx,
		pub.Method(method),
		pub.URL(httpx.JoinHostAndPath(_c.host, Logout.Route)),
		pub.RelativeURL(relativeURL),
		pub.Body(body),
		pub.Options(_c.opts...),
	)
}

// Demo is the signed-in support console where a customer asks natural-language banking questions.
func (_c Client) Demo(ctx context.Context, method string, relativeURL string, body any) (res *http.Response, err error) { // MARKER: Demo
	if method == "" {
		method = Demo.Method
	}
	if method == "ANY" {
		method = "POST"
	}
	return _c.svc.Request(
		ctx,
		pub.Method(method),
		pub.URL(httpx.JoinHostAndPath(_c.host, Demo.Route)),
		pub.RelativeURL(relativeURL),
		pub.Body(body),
		pub.Options(_c.opts...),
	)
}

// Demo is the signed-in support console where a customer asks natural-language banking questions.
func (_c MulticastClient) Demo(ctx context.Context, method string, relativeURL string, body any) iter.Seq[*pub.Response] { // MARKER: Demo
	if method == "" {
		method = Demo.Method
	}
	if method == "ANY" {
		method = "POST"
	}
	return _c.svc.Publish(
		ctx,
		pub.Method(method),
		pub.URL(httpx.JoinHostAndPath(_c.host, Demo.Route)),
		pub.RelativeURL(relativeURL),
		pub.Body(body),
		pub.Options(_c.opts...),
	)
}

// RunSupport is the single task of the Support workflow. It runs llm.core's ChatLoop as a subgraph with the
// Balance and Transactions tools and parses the model's final message into the structured verdict.
func (_c Executor) RunSupport(ctx context.Context, query string) (advice string, blockCard bool, risk int, err error) { // MARKER: RunSupport
	var out RunSupportOut
	err = marshalTask(ctx, _c.svc, _c.opts, _c.host, RunSupport.Method, RunSupport.Route, RunSupportIn{Query: query}, &out, _c.inFlow, _c.outFlow)
	return out.Advice, out.BlockCard, out.Risk, err // No trace
}

// Support is the durable workflow that answers a customer's banking question. Its RunSupport task runs the LLM
// tool-calling loop (ChatLoop) with the Balance and Transactions endpoints as tools and produces a structured
// verdict: advice text, whether the card should be blocked, and a 0-10 risk score. It is a workflow rather than a
// synchronous call because the multi-turn agent conversation routinely exceeds a single request's time budget;
// each turn is its own durable, independently-budgeted foreman step.
func (_c Executor) Support(ctx context.Context, query string) (advice string, blockCard bool, risk int, status string, err error) { // MARKER: Support
	if _c.runner == nil {
		return advice, blockCard, risk, "", errors.New("workflow runner not set, use WithWorkflowRunner")
	}
	var out SupportOut
	status, err = marshalWorkflow(ctx, _c.runner, _c.flowOptions, Support.URL(), SupportIn{Query: query}, &out)
	return out.Advice, out.BlockCard, out.Risk, status, err
}

// Support is the durable workflow that answers a customer's banking question. Its RunSupport task runs the LLM
// tool-calling loop (ChatLoop) with the Balance and Transactions endpoints as tools and produces a structured
// verdict: advice text, whether the card should be blocked, and a 0-10 risk score. It is a workflow rather than a
// synchronous call because the multi-turn agent conversation routinely exceeds a single request's time budget;
// each turn is its own durable, independently-budgeted foreman step.
func (_sg Subgraph) Support(ctx context.Context, query string) (advice string, blockCard bool, risk int, yield bool, err error) { // MARKER: Support
	var out SupportOut
	yield, err = marshalSubgraph(_sg.flow, Support.URL(), SupportIn{Query: query}, &out)
	if yield || err != nil {
		return advice, blockCard, risk, yield, err
	}
	return out.Advice, out.BlockCard, out.Risk, false, nil
}
