name: "Test"
on:
  push:
    branches:
      - "*"
      - "*/*"

jobs:
  test:
    runs-on: ubuntu-latest
    name: "Run tests"
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-go@v3
        with:
          go-version: '1.19.1'
        run: |
          'go version'
