---
# General
#
# host - The host name of the microservice
# description - A human-friendly description of the microservice
general:
  host: http.ingress.sys
  description: The HTTP Ingress microservice relays incoming HTTP requests to the NATS bus.

# Config properties
#
# signature - Func() (val Type)
# description - Documentation
# default - A default value (defaults to empty)
# validation - A validation pattern
#   str [a-zA-Z0-9]+
#   bool
#   int [0,60]
#   float [0.0,1.0)
#   dur (0s,24h]
#   set Red|Green|Blue
#   url
#   email
#   json
# callback - "true" to handle the change event (defaults to "false")
# secret - "true" to indicate a secret (defaults to "false")
configs:
  - signature: TimeBudget() (budget time.Duration)
    description: TimeBudget specifies the time out for incoming requests.
    default: 20s
    validation: dur [1s,5m]
  - signature: Port() (port int)
    description: Port is the HTTP port on which to listen for incoming requests.
    default: 8080
    validation: int [1,65535]

# Functions
#
# signature - Func(name Type, name Type) (name Type, name Type, httpStatusCode int)
# description - Documentation
# path - The subscription path
#   (empty) - The function name in lowercase
#   /path
#   /directory/
#   :123/path
#   :123/... - Ellipsis denotes the function name in lowercase
#   https://example.com:123/path
#   ^ - Empty path
# queue - The subscription queue
#   default - Load balanced (default)
#   none - Pervasive
functions:
  # - signature:
  #   description:

# Types
#
# name - All non-primitive types used in functions must be accounted for
# description - Documentation
# define - Define a new type with the specified fields (name: type)
# import - A path to another microservice that defines the type
types:
  # - name: DefinedType
  #   description:
  #   define:
  #     fieldName: Type
  # - name: ImportedType
  #   description:
  #   import: package/path/of/another/microservice

# Web handlers
#
# signature - Func()
# description - Documentation
# path - The subscription path
#   (empty) - The function name in lowercase
#   /path
#   /directory/
#   :123/path
#   :123/... - Ellipsis denotes the function name in lowercase
#   https://example.com:123/path
#   ^ - Empty path
# queue - The subscription queue
#   default - Load balanced (default)
#   none - Pervasive
webs:
  # - signature:
  #   description:

# Tickers
#
# signature - Func()
# description - Documentation
# interval - Duration between iterations (e.g. 15m)
# timeBudget - Duration to complete an iteration
tickers:
  # - signature:
  #   description:
  #   interval:
