# Code generated by cmd/genservice. DO NOT EDIT.

general:
  name: Kitchen
  hostname: kitchen.fixture
  description: Kitchen is a gencreds fixture exercising every detected call pattern.
  package: github.com/microbus-io/fabric/cmd/gencreds/testdata/kitchen
  modifiedAt: "2026-07-10T06:52:57Z"

configs:
  ApiKey:
    signature: ApiKey() (value string)
    description: ApiKey is a plain (non-secret) identifier used by the kitchen fixture.
  Threshold:
    signature: Threshold() (value string)
    description: Threshold caps the kitchen fixture's in-flight requests.
    validation: int [1,]
    default: 10
    callback: true

metrics:
  RequestsTotal:
    signature: RequestsTotal(value int, status string)
    description: RequestsTotal counts requests handled by the kitchen fixture, labelled by status.
    kind: counter
    otelName: kitchen_requests_total
  QueueDepth:
    signature: QueueDepth(value int)
    description: QueueDepth records the current depth of the kitchen queue.
    kind: gauge
    otelName: kitchen_queue_depth
    observable: true
  LatencySeconds:
    signature: LatencySeconds(value float64)
    description: LatencySeconds records request latency in seconds.
    kind: histogram
    buckets: [0.01, 0.05, 0.1, 0.5, 1, 5]
    otelName: kitchen_latency_seconds

outboundEvents:
  OnMyEvent:
    signature: OnMyEvent(note string) (ok bool)
    description: OnMyEvent is fired when something happens.
    method: POST
    route: :417/on-my-event

functions:
  MyFunc:
    signature: MyFunc(input string) (output string)
    description: MyFunc handles the MyFunc endpoint and exercises every call pattern.
    method: POST
    route: :443/my-func
  SelfPing:
    signature: SelfPing()
    description: SelfPing handles peer-to-peer self-pings.
    method: POST
    route: :444/self-ping
  AltHostFn:
    signature: AltHostFn()
    description: AltHostFn registers on a non-own host via the //host form.
    method: GET
    route: //alt.kitchen:0/alt-fn

inboundEvents:
  OnSomething:
    signature: OnSomething(detail string) (ok bool)
    description: OnSomething is the hook handler for weirdapi.OnSomething inbound events.
    package: github.com/microbus-io/fabric/cmd/gencreds/testdata/weird/weirdapi
